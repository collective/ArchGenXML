from zope import interface
from zope import component

class ArchetypeModuleView(object):
    """A view for a module with an archetype class in it.

    This is the view that is normally used for content types. By
    default, there is one class per module.
    """
    
    #interface.implements()

    pass
